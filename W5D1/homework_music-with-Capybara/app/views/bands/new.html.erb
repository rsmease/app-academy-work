<% unless flash.now[:errors].nil? %>
  <% flash.now[:errors].each do |error| %>
      <h2><%= error %></h2>
  <% end %>
<% end %>

<form class="login" action="<%= bands_url %>" method="post">
  <!-- <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>"> -->
  <%= token_tag nil %>
  <label for="name">Name</label>
  <input class="enter-input" id="name" type="text" name="band[name]" value="">
  <input class="submit-input" type="submit" value="Add Band">
</form>
